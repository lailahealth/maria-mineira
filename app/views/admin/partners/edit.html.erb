<% content_for :title, "Editar parceiro" %>

<p class="font-display text-sm font-semibold uppercase tracking-widest text-terracota">CMS</p>
<h1 class="mt-2 font-display text-3xl font-semibold tracking-tight text-vinho"><%= @partner.name %></h1>

<%= render "form", partner: @partner %>
